# CSS Notes

## The box model
Think of the content as a box with multiple layers.
- Margin > Border > Padding > Content.

## Size
There is `px` screen pixel.
There is `rem` relative to root (html font size).
There is `em` relative to parent font size.
